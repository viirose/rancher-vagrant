# /bin/sh

prlctl set server1 --on-window-close keep-running
prlctl set server2 --on-window-close keep-running
prlctl set server3 --on-window-close keep-running
prlctl set worker1 --on-window-close keep-running
prlctl set worker2 --on-window-close keep-running